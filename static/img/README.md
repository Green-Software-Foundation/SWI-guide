`logo.svg` and `favicon.svg` are structural placeholders (a plain
water-drop mark) so the site builds and the navbar/browser tab are not
empty. They are not final GSF branding — replace them with approved GSF
brand assets before this project leaves draft state.
